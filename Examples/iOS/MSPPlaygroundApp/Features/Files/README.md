# Files Feature

This feature owns the workspace file browser content.

It renders virtual MSP workspace paths such as `/`, `/notes.md`, and
`/docs/file.txt`. It does not own the left-side drag interaction; that belongs
to `Features/WorkspaceDrawer`.
